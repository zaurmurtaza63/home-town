# If you have a backend API running locally, set it here, e.g.
# REACT_APP_API_URL=http://localhost:5000
# Leaving this blank forces the frontend to use the built-in client-side mock auth
REACT_APP_API_URL=http://localhost:3000/
REACT_APP_API_URL=https://dream-home-backend-rust.vercel.app/api